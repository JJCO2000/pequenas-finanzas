import React from 'react'; import { Text, View } from 'react-native'; import type { GameComponentProps } from '@/core/game-runtime';
export function CoinCatcherGame({session,onFinish}:GameComponentProps){return <View style={{gap:12,padding:20}}><Text>Atrapa Monedas usa Skia + gestos en Android/iOS.</Text><Text>En web dejamos un fallback deliberado para que el proyecto no rompa el bundle.</Text><Text onPress={()=>onFinish({gameId:session.gameId,sessionId:session.sessionId,score:3,durationMs:1000,completed:true,metrics:{fallback:true}})}>Toca aquí para simular fin.</Text></View>}
