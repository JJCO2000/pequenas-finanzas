import React from 'react';import{StyleSheet,Text,View}from'react-native';import type{Wallet}from'@/core/domain/types';import{formatMoney}from'@/core/domain/money';import{colors,radii,spacing,typography}from'@/core/theme/tokens';
export function WalletSummary({wallet}:{wallet:Wallet|null}){if(!wallet)return null;return <View style={s.row}>{[['Disponible',wallet.availableCents],['Ahorro',wallet.savingsCents],['Inversión',wallet.investedCents]].map(([l,v])=><View key={String(l)} style={s.card}><Text style={s.label}>{l}</Text><Text style={s.value}>{formatMoney(Number(v))}</Text></View>)}</View>}
const s=StyleSheet.create({row:{flexDirection:'row',gap:spacing.sm,flexWrap:'wrap'},card:{flexGrow:1,minWidth:100,backgroundColor:colors.surface,borderRadius:radii.md,padding:spacing.md},label:{color:colors.inkMuted,fontWeight:'800'},value:{color:colors.forestDark,fontSize:typography.h2,fontWeight:'900'}});
