import React,{useState}from'react';import{StyleSheet,Text,TextInput,View}from'react-native';import{PFButton}from'@/features/shell/components/PFButton';import{colors,radii,spacing,typography}from'@/core/theme/tokens';
export function ParentGate({children}:{children:React.ReactNode}){const[a,b]=[7,4];const[input,setInput]=useState(''),[open,setOpen]=useState(false);if(open)return <>{children}</>;return <View style={s.card}><Text style={s.title}>Zona para adultos</Text><Text style={s.body}>Para continuar, resuelve: {a} × {b} = ?</Text><TextInput accessibilityLabel="Respuesta del adulto" value={input} onChangeText={setInput} keyboardType="number-pad" style={s.input}/><PFButton label="Entrar" onPress={()=>setOpen(Number(input)===a*b)}/><Text style={s.note}>MVP: este gate evita acceso accidental; no es autenticación de seguridad.</Text></View>}
const s=StyleSheet.create({card:{backgroundColor:colors.surface,borderRadius:radii.lg,padding:spacing.lg,gap:spacing.md},title:{fontSize:typography.h1,fontWeight:'900',color:colors.forestDark},body:{fontSize:typography.body,color:colors.ink},input:{backgroundColor:colors.white,borderWidth:2,borderColor:colors.creamStrong,borderRadius:radii.md,padding:spacing.md,fontSize:22},note:{color:colors.inkMuted,fontSize:typography.small}});
