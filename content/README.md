# Contenido fuera del código de gameplay
El contenido pedagógico debe vivir aquí o en `src/content` como módulos de datos; nunca incrustado dentro de la lógica de un minijuego.
`content/` conserva fuentes editoriales y borradores; `src/content/` contiene la versión tipada que consume la app.
